# 引き継ぎ書 — pin-game (Roblox / Rojoプロジェクト)

作成日時: 2026-09-16 19:37

---

## 1. 達成したこと

- ローカルPCのUSB（`D:\pin-game_backup`）にあったRobloxプロジェクト一式を、GitHubリポジトリ
  **`pankoha/roblox-game-project`** にアップロード（初回プッシュ）した。
- ローカルPCにGitをインストールし、GitHub認証（Personal Access Token）の使い方を確立した。
- Claude Codeのこのクラウドセッションに `pankoha/roblox-game-project` をpush権限付きで追加し、
  `/home/user/roblox-game-project` にクローン済み。次回セッションでもここから作業を再開できる状態。
- プロジェクトの構成を確認済み（下記「6. 参照すべきファイル」参照）。

## 2. 進行中のタスク

- **ゲームの実装そのものはまだ着手していない。** 現状は「開発環境をUSB→GitHub→Claude Codeへ移行する」
  という準備段階が完了しただけ。
- `src/shared/Script.server.lua` と `src/server/Script.server.lua` は、どちらも
  `print("Hello world!")` のみのテスト用の中身のまま（実装ゼロの状態）。
- `src/client/` フォルダは現状ファイルなし（空）。

## 3. 次にやるべきこと（優先順位順）

1. **ゲームの企画・仕様をユーザーから聞き取る** — ジャンル、コア機能、既存の `.rbxl`
   （Studio側）にどんな要素が既に入っているかをまず確認する（このClaude Codeセッションからは
   `.rbxl` バイナリの中身は見えないため、Studio側での説明が必要）。
2. 聞き取った内容をもとに `src/server`・`src/shared`・`src/client` にLuaスクリプトを実装。
3. ローカルPC側で `rojo serve` を起動し、Roblox Studioと同期しながら動作確認（この作業はユーザーの
   ローカルPCで行う必要があり、Claude Code単独では完結しない）。
4. 実装が進んだら、都度GitHubにコミット・プッシュして記録を残す。

## 4. 変更したファイル一覧

このセッションでの変更は以下のみ（実装コードの変更はまだなし）:

| ファイル | 変更概要 |
|---|---|
| `handoff/2026-09-16_19-37_handoff.md` | 本引き継ぎ書を新規作成 |

USB→GitHubの初回プッシュで追加されたファイル（既存）:
- `.gitignore`（`*.rbxl` `*.rbxlx` `.aftman/` `.rojo/` を除外）
- `README.md`（Rojoセットアップ手順）
- `aftman.toml`（Rojoツールチェーン定義）
- `default.project.json`（Rojo同期設定）
- `src/shared/Script.server.lua`
- `src/server/Script.server.lua`

## 5. 未解決の論点（ユーザーへの確認事項）

- **ゲームの内容が未定義。** ジャンル・目的・既存のマップ/パーツ構成など、`.rbxl`
  ファイル（Roblox Studio側）にある情報をこのセッションでは読み取れない。次回、要件を
  言葉で伝えてもらう必要がある。
- ローカルPCの **Windowsマイクロソフトアカウントへの切り替え作業は保留中**（本題とは無関係と
  判断し中断。`roborobo1010@outlook.com` の新規作成、`pin2hisaki2023@gmail.com` でのパスワード
  エラーが起きていた）。再開が必要か、そのままでよいかは未確認。
- GitHub認証に使ったPersonal Access Token（pankohaアカウント用）は、作業中にスクリーンショットで
  チャットに写り込んだため、**無効化と再発行を推奨した**。実際に無効化されたかどうかは未確認。
- 今後 `rojo serve` を使った実機動作確認は、Claude Codeのクラウド環境では行えないため、
  ユーザーのローカルPC（Roblox Studioが入っている環境）で作業してもらう前提になる。

## 6. 参照すべきファイル・ドキュメント

- `/home/user/roblox-game-project/README.md` — Rojoのセットアップ手順、開発フロー全体
- `/home/user/roblox-game-project/default.project.json` — Rojoの同期先マッピング
  （`src/shared`→`ReplicatedStorage/Shared`、`src/server`→`ServerScriptService/Server`、
  `src/client`→`StarterPlayer/StarterPlayerScripts/Client`）
- `/home/user/roblox-game-project/src/` — 実装対象のLuaコード一式（現状ほぼ空）

## 7. 最初に読むべきファイル（次回セッション開始時）

1. **この引き継ぎ書** (`handoff/2026-09-16_19-37_handoff.md`)
2. `README.md`（プロジェクトのセットアップ・開発フロー確認用）
3. `src/server/Script.server.lua` と `src/shared/Script.server.lua`（現状の実装状態確認用）

---

## 新しいセッションでの再開方法

1. 新しいチャットを開始する
2. 「pankoha/roblox-game-project の handoff/2026-09-16_19-37_handoff.md
   を読んでから続きをお願いします」と伝える
3. リポジトリがまだセッションに追加されていない場合は、Claude側が自動でGitHub追加を提案します
